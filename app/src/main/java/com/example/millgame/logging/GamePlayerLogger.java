package com.example.millgame.logging;

import javax.swing.*;
import java.nio.file.Path;
import java.util.logging.Level;

/*
public class GamePlayerLogger extends GameLogger{
    private JTextArea logPanel;
    protected GamePlayerLogger(String name, Path logfile, Level loglevel){
        super(name, logfile, loglevel);
    }

    @Override
    public void log(Level level, String message){
        super.log(level, message);
        // SEND TO logPanel
    }

}
*/